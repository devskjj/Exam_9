server:
  port: 8089

spring:
  application:
    name: Exam9
  data:
    web:
      pageable:
        serialization-mode: VIA_DTO

  messages:
    basename: messages
    encoding: UTF-8

  mail:
    host: smtp.gmail.com
    username: devskjj@gmail.com
    password: kfcb dcno zsbi iipr
    port: 587

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:h2:./db/test;AUTO_SERVER=TRUE
    username: Denis
    password: admin
    driver-class-name: org.h2.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yaml

  jpa:
    hibernate:
      ddl-auto: validate

  mvc:
    static-path-pattern: /static/**
    log-request-details: true

  freemarker:
    expose-session-attributes: true

logging:
  level:
    root: info

  file:
    path: log